/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        red: '#FF0000', // Red color (replace with your preferred shade of red)
        blue: '#007BFF', // Blue color (replace with your preferred shade of blue)
        green: '#00CC00', // Green color (replace with your preferred shade of green)
        // Add more custom colors as needed
      },
    },
    // Rest of the theme configuration
  },
  // Rest of the config
};

module.exports = {
  theme: {
    extend: {
      fontSize: {
        'xs': '0.75rem', // Extra small font size
        'sm': '0.875rem', // Small font size
        'base': '1rem', // Default base font size
        'lg': '1.125rem', // Large font size
        'xl': '1.25rem', // Extra large font size
        // Add more custom font sizes as needed
      },
    },
    // Rest of the theme configuration
  },
  // Rest of the config
};

module.exports = {
  theme: {
    extend: {
      spacing: {
        '2': '0.5rem', // 0.5 rem spacing
        '4': '1rem', // 1 rem spacing
        '6': '1.5rem', // 1.5 rem spacing
        '8': '2rem', // 2 rem spacing
        // Add more custom spacing as needed
      },
    },
    // Rest of the theme configuration
  },
  // Rest of the config
};
